# Snow Deep DB - Production Requirements
# AWS EC2 + RDS(PostgreSQL) + ALB Environment

# Django and core dependencies
Django>=4.2.0,<5.0.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0

# Data processing and ML
pandas>=2.0.0
prophet>=1.1.4
plotly>=5.14.0
scikit-learn>=1.3.0
numpy>=1.24.0

# Web server
gunicorn>=21.0.0
whitenoise>=6.5.0

# Caching (optional but recommended)
redis>=4.5.0
django-redis>=5.2.0

# Production utilities
django-extensions>=3.2.0
django-debug-toolbar>=4.0.0  # Remove in production

# AWS SDK (for S3, SES integration)
boto3>=1.26.0
django-storages>=1.13.0

# Monitoring and logging
sentry-sdk>=1.32.0

# Security
django-cors-headers>=4.0.0

